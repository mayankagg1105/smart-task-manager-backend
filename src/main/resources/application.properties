#spring.application.name=taskmanager
#
## Server
#server.port=8888
#server.servlet.context-path=/taskmanager
#
## JDBC
#spring.datasource.url=jdbc:mysql://localhost:3306/taskmanager?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=root
##spring.datasource.password=anmol
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## JPA & Hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#
#

spring.application.name=taskmanager

# Server (DISABLED: Let's use default 8080 and root path / for Cloud)
# server.port=8888
# server.servlet.context-path=/taskmanager

# JDBC (These will be overwritten by Railway Variables, so they are safe to keep)
spring.datasource.url=jdbc:mysql://localhost:3306/taskmanager?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
# spring.datasource.password=  <-- Good practice: don't keep passwords here
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# AI Key (Move this to Railway Variables for security!)
gemini.api.key=${GEMINI_API_KEY}